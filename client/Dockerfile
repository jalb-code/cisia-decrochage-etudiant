# Maquette de présentation — page statique servie par nginx. Hors périmètre livré : le
# service, Prometheus et Grafana sont le dispositif ; cette page sert la démonstration.
FROM nginx:1.27-alpine
COPY static /usr/share/nginx/html
EXPOSE 80
