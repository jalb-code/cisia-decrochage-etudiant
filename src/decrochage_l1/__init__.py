"""Package `decrochage_l1` — logique réutilisable du cas d'usage.

Le notebook n'héberge aucune logique : il appelle ce package, installé en éditable
par `uv sync`. Un même code sert donc l'exploration et tout usage ultérieur, ce
qui interdit qu'ils divergent.

Sous-package `data` : accès aux fichiers, profilage et primitives de mise en forme.
"""

__version__ = "0.1.0"
